pipeline{
  agent any
  stages{
    stage('hello'){
      steps{
        echo "demo of pipeline from poll scm."
      }
    }
      stage('build')
      {
        steps{
          sh 'cat file1'
        }
    }
  }
}
